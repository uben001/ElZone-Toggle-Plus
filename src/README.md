# Camera source

`fcscale.S` implements the Fix 11 overlay and button gestures.
`fcscale_native.inc.S` contains the native legend data generated by the builder.

See the repository README for supported firmware and rebuilding instructions.
